export interface Todo {
  id: number;
  content: string;
}

export interface Meta {
  totalCount: number;
}

export type Player = {
  id: string;
  firstName: string;
  lastName: string;
  age: number
}
